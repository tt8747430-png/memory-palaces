# PrimeNG 21 and lucide-angular 1.x peer on Angular <=21; app runs Angular 22.
# Angular keeps previous-major libraries compatible (stable Ivy linker).
# Remove once PrimeNG 22 stable + lucide-angular Angular-22 peers ship.
legacy-peer-deps=true
