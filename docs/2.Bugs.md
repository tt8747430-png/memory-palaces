# Problems

GENERAL:

REFACTORINGS:

SETTINGS:

8. i cant change the learning algorithm in the subdeck, it must be disabled and a message like this must come /Users/kristianbraila/Downloads/IMG_3483.PNG
1. when deleting a folder, it also must delete all decks that it constains
2. when i move the deck out of the folder all of its' subdecks become decks alongside with the main deck. This is a bug and the subdecks must remain subdecks
3. the buttons in the profile page must look like a button and have more button look but also blend with other a little bit, the day streak button i mean. 
4. the x close button in the select bar mode should be in the corner like it is in the actions from the select toolbar editor settings, but must be in the main frame and not in the actions frame border, and not like now on the left side of the toolbar. 
5. the actions in the edit select toolbar when dragging and reordering them should alos be elevated with the x button in th ecorner and this should be for all the edit pages and drag and drop actions across the app where this is relevant
6. in the search cards in the deck details page, the search bar has no padding on top from the header bar but it must have





1. all the  actions that require online check must be disabled: like the password change it must be checked in order to successfully change so offline we cant do it. but what do you say about the actions like the  deleting a folder because i can delete it localy and after the user gets internet we can sync the data with our database and send a request to delete a folder or other things that doesnt necessary need internet at the moment of execution.

also what means this your response:
Beyond the audit's list, three things it got wrong — I checked rather than implemented:
- import-review reads only its own local draft store, so it needed no readiness gate.
- The avatar upload already handles offline deliberately (uploadInlineImage never throws); a gate would have broken an offline-first feature.
- Account deletion is entirely local and touches no gateway.



4. G (first-paint bytes). The heavy deps are now separate cacheable chunks (persistence 294 kB, supabase 209 kB, react 190 kB) so an app-code deploy doesn't invalidate them in the precache — a caching win, not a first-paint one. Every preload in dist/index.html is a genuine entry dependency; getting RxDB off the critical path means deferring createServices() behind the splash and making services awaited instead of a module-level singleton. That's an architecture change and wants its own piece of work.









OTHER: 
8. we dont have onboarding and guest account lacks customizability
1. Appheader should be a component and all the components should be maybe just a implementation of the content inside of this header and not reimplement the whole template and shell, just the content inside the header like the buttons and so on .this should be across all the app

FEATURES:
2. improve the paste notes add bible prefill and better admin mode, and the edit cards sheet, 
3. add posibility to submit bugs 
1. Extenstions Gallery: Memory Palaces, Bible 
1. Journey in palaces locies 
2. Journey stations editing and connecting
3. Add the actual palace locations and real palace image
