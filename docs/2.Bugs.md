# Problems

GENERAL:

Needs you before the server side works
- Store two secrets in Vault: project_url (https://dimdowcidkuuxmfdizqt.supabase.co) and secret_key (an sb_secret_... key from Settings -> API Keys). Without them invoke_purge_account logs a notice and returns, so the nightly purge does nothing. Everything else is live.
- To run the purge integration test, set SUPABASE_TEST_SECRET_KEY to that same sb_secret_... key, alongside the existing test env vars. The legacy service_role JWT will not do: purge-account checks the key it is sent against SUPABASE_SECRET_KEYS.
- Nothing has been tried on a device or in a browser, only in jsdom.

Done on the project
- All nine migrations applied, and the hosted history repaired first: its rows were the six phase9 entries under different timestamps, plus a phase9_rls_revoke_anon with no local file, so db push would have replayed all of them. Every row now byte-matches its file.
- Both buckets are private, with an owner predicate on read. The "clients can list all files in this bucket" advisor is gone.
- history and account_deletions exist, with RLS, Realtime, and push_documents accepting history.
- pg_cron and pg_net are installed, pg_net into extensions rather than public, which is what the "Extension in Public" advisor wanted. Both jobs are scheduled: trim-history 03:17, purge-accounts 03:41.
- Both Edge Functions are deployed with verify_jwt = false, authorizing in code: the app is on publishable keys, and the platform's JWT check only understands the legacy ones. Smoke-tested: POST with no key -> 403, with a wrong key -> the gateway's own 401, GET -> 405, and request-account-deletion with no bearer -> 401.
- The advisors left are not ours: rls_auto_enable is Supabase's own event-trigger function (it returns event_trigger, so REST cannot call it), leaked-password protection is a Dashboard Auth setting, and the three "unused index" notes are on indexes nothing has queried yet.

Pushed back, no change
- Scope creep from the file splits and Lexend subset: the approved spec requires them.
- outcome.kind switched in four places: the spec says every caller handles all five outcomes, which overrides the smell baseline.
- Echo filter and the second peek flagged as unrequested: kept, now corrected. Without them the banner lights up after every Sync.
- Raw runner.error in the banner: the spec's table says "Failed - Retry, with the reason", and auth-error-copy.ts already fixes the same rule - a known code gets copy, anything else keeps the provider's sentence, which beats a generic apology.
- pendingByCollection is not named select... on purpose: it builds a fresh object every call, so it could never compare equal as a selector. Screens call it inside a useMemo.

Still open
- Nothing has been checked on a device yet.
- The two Vault secrets, above. That is the whole of what is left on the server.







REFACTORINGS:

SETTINGS:

1. al the actions frmo the menu cards should be available also in swipe actions and improve the actions maybe we also we need other useful actions as well like manually setting the grade or begin the study with this card or from this card
2. add a carrousel preview, like in photos app, in between the two button previous and next card. and the edit move to the header the next to the menu button 
/Users/kristianbraila/Downloads/IMG_3526.png








 Lexend still imports whole (Vietnamese subset included). The reviewer was right that it contradicts my fonts.css header, but the honest fix is to stop overclaiming in the header, not to quietly trim the app's own typeface in a card-styles diff — Lexend renders deck names and card text with no per-card choice behind it, so cutting its subsets changes what the whole app can render. Noted in the file as a separate call.


1. all the  actions that require online check must be disabled: like the password change it must be checked in order to successfully change so offline we cant do it. but what do you say about the actions like the  deleting a folder because i can delete it localy and after the user gets internet we can sync the data with our database and send a request to delete a folder or other things that doesnt necessary need internet at the moment of execution.

also what means this your response:
Beyond the audit's list, three things it got wrong — I checked rather than implemented:
- import-review reads only its own local draft store, so it needed no readiness gate.
- The avatar upload already handles offline deliberately (uploadInlineImage never throws); a gate would have broken an offline-first feature.
- Account deletion is entirely local and touches no gateway.

4. G (first-paint bytes). The heavy deps are now separate cacheable chunks (persistence 294 kB, supabase 209 kB, react 190 kB) so an app-code deploy doesn't invalidate them in the precache — a caching win, not a first-paint one. Every preload in dist/index.html is a genuine entry dependency; getting RxDB off the critical path means deferring createServices() behind the splash and making services awaited instead of a module-level singleton. That's an architecture change and wants its own piece of work.









OTHER: 
8. we dont have onboarding and guest account lacks customizability
1. Appheader should be a component and all the components should be maybe just a implementation of the content inside of this header and not reimplement the whole template and shell, just the content inside the header like the buttons and so on .this should be across all the app

FEATURES:
2. improve the paste notes add bible prefill and better admin mode, and the edit cards sheet, 
3. add posibility to submit bugs 
1. Extenstions Gallery: Memory Palaces, Bible 
1. Journey in palaces locies 
2. Journey stations editing and connecting
3. Add the actual palace locations and real palace image
4. Goals and habits and their levels
5. Bible read with the extension. a menu in bottom nav and auto add to the cards