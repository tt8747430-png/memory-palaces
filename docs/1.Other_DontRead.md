1. review the pages structures and theme.css and other appscreen structure and look for overlapping structures colliding, things that are not used, outdated, broken, wrong, look at the events que and so on.  find broken styles broken tailwind that doesnt work, find all insconsistencies, use /superpowers and other bug finding skills and other best practices and skills 


2. "Be extremly concise. Sacrifise grammar for the sake of consision." - Put this on top of the CLAUDE.md and document
   it everywhere you can.


Audit-only. Do NOT fix anything. Output findings; I choose what gets fixed.

SCOPE — Mindscape UI + app shell, whole repository and also this explicitly:
  src/styles/{tokens,theme,index}.css · src/shared/ui/** (AppScreen, header/, Sheet,
  SpeedDial, FooterBar, SelectToolbarDock) · src/widgets/** · src/pages/**
  src/app/{router,composition-root,RootLayout}.* · src/shared/lib/{event-bus,events,motion,
  keyboard-viewport,gestures}.ts

METHOD — read docs/CODE_STYLE.md (§4, §7–11), docs/adr/0001, docs/adr/0002,
docs/UBIQUITOUS_LANGUAGE.md, docs/DECK_SETTINGS_UI_STATUS.md BEFORE judging anything.
Use skills: superpowers:systematic-debugging (any suspected bug), web-design-guidelines
(a11y/UX pass), vite-react-best-practices (perf/bundle). Verify with grep/tests, not memory. also use other needed skills and best practices like vercel skill. and also check whether the code styles and adrs are actually correct. also use the mattpocock skills like /codebase-design and other like grilling and asking use and also brainstorming for fingding inconsistencies

EVIDENCE RULE — every finding needs file:line, the concrete trigger (device, route,
state), and the observable wrong outcome. If you cannot name the trigger, it goes in a
separate "unverified suspicion" list. No "consider refactoring". No style opinions.

PASSES (run all, report separately):
 1. Token layer — tokens declared and never referenced; referenced and never declared;
    defined only on :root with no [data-theme='dark'] counterpart; duplicate values under
    two names; theme.css rules overridden elsewhere and dead.
 2. Tailwind — v3 classes that no-op in v4; arbitrary values (rounded-[Npx], z-[N], px-[N])
    that bypass a token that exists; dynamically built class strings the compiler never
    sees; cn() calls where two conflicting utilities both survive.
 3. Stacking + insets — z-index ladder across bottom-nav / SpeedDial / SelectToolbarDock /
    Sheet / ActionSheet / FooterBar / toasts: overlaps, wrong order, unreachable controls.
    Double-counted env(safe-area-*). --app-height / --kb-range misuse. Anything that moves
    the header when the keyboard opens (ADR 0002 says nothing may). and also all other controls that need revision
 4. Screen frame — AppScreen/ScreenGutter/Header used inconsistently across the 37 pages:
    two paddings for the same slot, a page hand-rolling what the frame provides, a page
    missing the frame. List the outliers, not the conforming majority. check all. 
 5. Event bus — src/shared/lib/events.ts: events published with no subscriber; subscribers
    for events never published; subscriptions without cleanup; emits during render.
 6. Wiring — stores in composition-root missing start(); screens reading a store without
    gating on selectIsReady; writes done outside features/**; deep cross-slice imports that
    dodge a barrel; barrel exports nothing imports.
 7. Screen states — for every route: loading, error, empty, OFFLINE. Name the ones missing
    a state, and which.
 8. i18n — literal user-facing strings not in shared/i18n/locales/en.ts; keys defined and
    unused; keys referenced and missing.
 9. Persistence back-compat — app/persistence/schemas.ts: type fields absent from schema,
    schema version bumped without a migration, required field added without a default.
10. Dead weight — components/pages/routes/assets nothing reaches; commented-out blocks;
    props accepted and never read.

OUTPUT — write .scratch/ui-audit-2026-09-05/findings.md, grouped by pass, each finding:
  severity (broken / inconsistent / dead / risky) · file:line · trigger · wrong outcome ·
  smallest fix. Sort by severity within group. Then in chat: the 10 worst, one line each.
Run npm run typecheck && npm run lint && npm run test first and report the baseline.
